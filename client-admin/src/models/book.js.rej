diff a/src/models/book.js b/src/models/book.js	(rejected hunks)
@@ -1,2 +1,2 @@
-import {fetchBookList} from '@/services/bookApi'
-import {message} from 'antd'
+import { fetchBookList } from '@/services/bookApi';
+import { message } from 'antd';
@@ -12,4 +14 @@ const BookModel = {
-    *fetchBookList({payload},{call,put}){
-      const response=yield call(fetchBookList,payload)
-
-      if(response.code<=0){
+      if (response.code <= 0) {
@@ -21,4 +20,3 @@ const BookModel = {
-        type:'fetchBookListSuccess',
-        payload:response.data,
-      })
-
+        type: 'fetchBookListSuccess',
+        payload: response.data,
+      });
@@ -29,2 +27 @@ const BookModel = {
-
-    fetchBookListSuccess(state, {payload}) {
+    fetchBookListSuccess(state, { payload }) {
@@ -35,2 +32 @@ const BookModel = {
-    }
-
+    },
@@ -38,4 +34,2 @@ const BookModel = {
-
-
-}
-export default BookModel
+};
+export default BookModel;
